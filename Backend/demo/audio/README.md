# Demo Audio

Place the manually captured sample generation here:

- `sample_generation.wav`

Use this folder only for normal text-to-speech generation output. Large local audio files are ignored by git.
